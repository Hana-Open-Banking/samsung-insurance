spring:
  profiles:
    active: local

#  datasource:
#    url: jdbc:mysql://localhost:3306/insurance_db?useSSL=false&allowPublicKeyRetrieval=true
#    username:
#    password:
#    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

logging:
  level:
    org.hibernate.SQL: debug
